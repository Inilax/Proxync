{
  "name": "proxync",
  "private": true,
  "version": "0.1.8",
  "description": "Proxync — instant tunnel + collaboration workspace",
  "workspaces": [
    "packages/desktop"
  ],
  "scripts": {
    "dev": "npm run dev --workspace=packages/desktop",
    "build": "npm run build --workspace=packages/desktop"
  },
  "engines": {
    "node": ">=20.0.0",
    "npm": ">=10.0.0"
  }
}